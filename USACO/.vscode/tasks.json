{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Run with Header (Windows)",
      "type": "shell",
      "command": "cmd",
      "args": [
        "/c",
        "g++ -std=c++17 -Wall -DLOCAL -include \"${fileDirname}/cp_header.h\" \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}.exe\" && \"${fileDirname}/${fileBasenameNoExtension}.exe\""
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}